server:
  port: 8082

spring:
  application:
    name: demand-service
  data:
    mongodb:
      uri: mongodb://localhost:27017/demands_db
  redis:
    host: localhost
    port: 6379

gemini:
  api:
    key: ${GEMINI_API_KEY:default-key-for-local-dev}
